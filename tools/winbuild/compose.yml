services:
  build:
    image: win-hedgewarsbe
    working_dir: /
    volumes:
      - ../../:/src
      - ./out:/build-Hedgewars
    environment:
      - BUILD_SERVER
    command: ["wine", "cmd", "/c", "z:/src/tools/winbuild/build-hedgewars.cmd"]

